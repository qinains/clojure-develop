SSL_get_fd(3)                       OpenSSL                      SSL_get_fd(3)



NNAAMMEE
       SSL_get_fd - get file descriptor linked to an SSL object

SSYYNNOOPPSSIISS
        #include <openssl/ssl.h>

        int SSL_get_fd(const SSL *ssl);
        int SSL_get_rfd(const SSL *ssl);
        int SSL_get_wfd(const SSL *ssl);

DDEESSCCRRIIPPTTIIOONN
       _S_S_L___g_e_t___f_d_(_) returns the file descriptor which is linked to ssssll.
       _S_S_L___g_e_t___r_f_d_(_) and _S_S_L___g_e_t___w_f_d_(_) return the file descriptors for the
       read or the write channel, which can be different. If the read and the
       write channel are different, _S_S_L___g_e_t___f_d_(_) will return the file descrip-
       tor of the read channel.

RREETTUURRNN VVAALLUUEESS
       The following return values can occur:

       -1  The operation failed, because the underlying BIO is not of the cor-
           rect type (suitable for file descriptors).

       >=0 The file descriptor linked to ssssll.

SSEEEE AALLSSOO
       _S_S_L___s_e_t___f_d(3), _s_s_l(3) , _b_i_o(3)



1.0.0g                            2005-03-30                     SSL_get_fd(3)
