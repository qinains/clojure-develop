SSL_get_psk_identity(3)             OpenSSL            SSL_get_psk_identity(3)



NNAAMMEE
       SSL_get_psk_identity, SSL_get_psk_identity_hint - get PSK client iden-
       tity and hint

SSYYNNOOPPSSIISS
        #include <openssl/ssl.h>

        const char *SSL_get_psk_identity_hint(const SSL *ssl);
        const char *SSL_get_psk_identity(const SSL *ssl);

DDEESSCCRRIIPPTTIIOONN
       _S_S_L___g_e_t___p_s_k___i_d_e_n_t_i_t_y___h_i_n_t_(_) is used to retrieve the PSK identity hint
       used during the connection setup related to SSL object ssssll. Similarly,
       _S_S_L___g_e_t___p_s_k___i_d_e_n_t_i_t_y_(_) is used to retrieve the PSK identity used during
       the connection setup.

RREETTUURRNN VVAALLUUEESS
       If non-NNUULLLL, _S_S_L___g_e_t___p_s_k___i_d_e_n_t_i_t_y___h_i_n_t_(_) returns the PSK identity hint
       and _S_S_L___g_e_t___p_s_k___i_d_e_n_t_i_t_y_(_) returns the PSK identity. Both are NNUULLLL-ter-
       minated. _S_S_L___g_e_t___p_s_k___i_d_e_n_t_i_t_y___h_i_n_t_(_) may return NNUULLLL if no PSK identity
       hint was used during the connection setup.

       Note that the return value is valid only during the lifetime of the SSL
       object ssssll.



1.0.0g                            2006-03-11           SSL_get_psk_identity(3)
